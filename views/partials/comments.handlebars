<div class="comments">
  {{!-- use keyword 'this' to refer to the parent array --}}
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{!-- implement the format_date helper function --}}
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>